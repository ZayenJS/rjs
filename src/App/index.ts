import logger from '../Logger';
import configFile from '../ConfigFile';

export class App {
  public static initProject = async () => {};

  public createReactApp = () => {};

  public createNextApp = () => {};
}

export default new App();
